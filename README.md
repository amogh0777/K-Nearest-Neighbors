# K-Nearest-Neighbors

A simple implementation of the K-Nearest Neighbors (KNN) algorithm for classification using Python and Scikit-learn.
This project demonstrates how to build, train, and evaluate a KNN model on sample datasets such as the Iris dataset.
